<Window x:Class="TaxiAutoClicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaxiAutoClicker"
        mc:Ignorable="d"
        Title="TaxiAutoClicker" FontSize="14" Height="300" Width="250" ResizeMode="NoResize" Loaded="MainWindow_OnLoaded" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Margin="15">
            <Button Height="35" Content="Запустить TaxiAutoClicker" ToolTip="Автокликер будет запущен в каждом открытом окне NoxPlayer" Click="LaunchAutoClicker_Click"></Button>
        </StackPanel>
        <StackPanel Margin="15 5" VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
            <TextBlock Text="X: "/>
            <TextBlock Name="XTextBlock" Width="30"/>
            <TextBlock Width="5"/>
            <TextBlock Text="Y: "/>
            <TextBlock Name="YTextBlock" Width="30"/>
        </StackPanel>
    </Grid>
</Window>
